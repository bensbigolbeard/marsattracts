!!! html
%head
  %title MarsAttracts
  = stylesheet_link_tag    "application", media: "all" 
  = javascript_include_tag "application" 
  = favicon_link_tag
  = csrf_meta_tags
  %script{ :src => "https://ajax.googleapis.com/ajax/libs/angularjs/1.2.9/angular.min.js"}

%body#background
  #midground
  #foreground
  = yield
  :javascript
    function DisplayCtrl($scope, $http){
      $scope.arr = ["test1","test2"]
      $http.get('http://localhost:8000/origins.json').success(function(data){
          $scope.arr = data;
      });
    }

